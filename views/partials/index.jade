div
  | Filter: 
  input.filter(ng-model='query')
br

div(ng-controller='ModalDemoCtrl')
  script(type='text/ng-template', id='myModalContent.html')
    div.modal-header
      h3.modal-title asdf
    div.modal-body
    div.modal-footer
  botton(ng-click='open()')
    asdf

  div.book-card(ng-repeat='book in books | filter:query', ng-click='open()')
    span.title
      a(href='{{book.douban_url}}', target='_blank') {{book.title}}
    br
    span.author
      {{book.author}}
